#include "main.h"
/**
 * _strlen - prints the string's length
 * @s: pointer to the string
 * Return: length of a string
 */
int _strlen(char *s)
{
	int count = 0;

	while (*str++)
	{
		count++;
	}
	return (count);
}
